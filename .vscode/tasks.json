{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Run DEV",
      "type": "shell",
      "command": "rm -rf node_modules/.cache && npm run dev ",
      "windows": {
        "command": "set PATH=C:\\Users\\chemo\\AppData\\Roaming\\nvm\\v18.17.1;%PATH% && set NODE_OPTIONS=--openssl-legacy-provider && rd /s /q node_modules\\.cache 2>nul & npm run dev"
      },
      "presentation": {
        "reveal": "always",
        "panel": "new"
      },
      "runOptions": { "runOn": "folderOpen" }
    }
  ]
}
